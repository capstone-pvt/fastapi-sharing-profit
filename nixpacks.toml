# Force Railway to use Dockerfile instead of Railpack/Nixpacks
[phases.setup]
# Empty — tells Nixpacks to skip auto-detection

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port ${PORT:-8000} --workers 1 --timeout-keep-alive 300"
